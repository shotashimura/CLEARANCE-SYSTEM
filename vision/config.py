# ============================================================================
# ArUco 検出の設定。
# ----------------------------------------------------------------------------
# カメラを差し替えるとき（ノートPC → 本番 Logicool BRIO）は、基本ここだけ編集。
# 検出ロジック(detect.py)・マーカー生成(generate_markers.py)は触らない。
# ============================================================================

# --- カメラ ---------------------------------------------------------------
# ノートPC内蔵カメラ = 0。USBカメラ(BRIO)を挿すと 0 or 1（要確認）。
# どの番号か分からなければ detect.py を --list で起動して確認。
CAMERA_INDEX = 0
FRAME_WIDTH = 1280
FRAME_HEIGHT = 720

# --- マーカー -------------------------------------------------------------
# 使う辞書（マーカーの種類）。4x4・50種で十分。
ARUCO_DICT = "DICT_4X4_50"
# 5台ぶん。marker ID = suitcaseId（fleet.js の 1..5 に対応させる）。
MARKER_IDS = [1, 2, 3, 4, 5]
# 印刷したマーカーの一辺の実寸 [mm]。pose（実寸の位置・距離）推定に使う。
# Step1（検出と画面内位置）では厳密でなくてよい。印刷後に実測して合わせる。
MARKER_LENGTH_MM = 80
# 印刷用の解像度
PRINT_DPI = 300

# --- カメラキャリブレーション（任意） -------------------------------------
# レンズ歪み補正・実寸pose推定に使う。無ければ None（検出は動く／pose精度は粗い）。
# 本番 BRIO では広角の歪みが大きいので、別途キャリブして "calib_brio.npz" などに差し替える。
CALIBRATION_FILE = None
